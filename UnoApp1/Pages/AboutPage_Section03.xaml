<UserControl x:Class="UnoApp1.Views.AboutPage_Section03" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition x:Name="ColDef" />
        </Grid.ColumnDefinitions>
        <StackPanel>
            <TextBlock x:Uid="GTP871" Style="{StaticResource MediumTextBlock}" />
            <TextBlock x:Uid="DFG165" Margin="0,24,0,0" Style="{StaticResource MediumLarginoTextBlock}" />
            <TextBlock x:Uid="DBV176" Margin="0,12,0,0" Style="{StaticResource MediumTextBlock}" />
            <TextBlock x:Uid="GFD103" Margin="0,16,0,0" Style="{StaticResource MediumTextBlock}" />
        </StackPanel>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ColDef.Width" Value="0" />
                    </VisualState.Setters>
                </VisualState>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1008" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="ColDef.Width" Value="300" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</UserControl>
