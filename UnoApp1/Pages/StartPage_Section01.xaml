<UserControl x:Class="UnoApp1.Views.StartPage_Section01" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<StackPanel>
		<TextBlock x:Name="PART_Header" Text="{x:Bind Header}"  />
		<TextBlock x:Name="PART_Text" Text="{x:Bind Text}" />
		<VisualStateManager.VisualStateGroups>
			<VisualStateGroup>
				<VisualState>
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="0" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
                        <Setter Target="PART_Header.Style" Value="{StaticResource MediumLargeTextBlock}" />
                        <Setter Target="PART_Text.Margin" Value="0,16,0,0" />
                        <Setter Target="PART_Text.Style" Value="{StaticResource SmallTextBlock}" />
                    </VisualState.Setters>
				</VisualState>
				<VisualState>
					<VisualState.StateTriggers>
						<AdaptiveTrigger MinWindowWidth="1008" />
					</VisualState.StateTriggers>
					<VisualState.Setters>
                        <Setter Target="PART_Header.Style" Value="{StaticResource ExtraLargeTextBlock}" />
                        <Setter Target="PART_Text.Margin" Value="0,24,0,0" />
                        <Setter Target="PART_Text.Style" Value="{StaticResource MediumTextBlock}" />
                    </VisualState.Setters>
				</VisualState>
			</VisualStateGroup>
		</VisualStateManager.VisualStateGroups>
	</StackPanel>
</UserControl>
